{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
    {# ✅ Messages flash de succès (affichés après l'inscription, par exemple) #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
    {% endfor %}

<div class="text-center mt-5">
    <h1>Bienvenue sur QuizMaster!</h1>
    <br>
    <h3 class="mt-3">Teste ton cerveau 🧠, explore des univers 🌍, progresse chaque jour 🚀</h3>
    <br>
    <a href="{{ path('accueil_list_niveaux') }}" class="btn btn-outline-turquoise btn-lg mt-4">
        Commencer
    </a>
</div>

<div class="d-flex justify-content-center mt-4">
    <img src="{{ asset('assets/images/quiz-show.svg') }}" 
         alt="Illustration quiz" 
         class="img-fluid" 
         style="max-width: 480px;">
</div>
{% endblock %}
